{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clean up",
            "type": "shell",
            "command": "echo '@' > query.txt; : > resources/input.json; : > resources/output.json; : > resources/view.csv",
            "problemMatcher": []
        }
    ]
}